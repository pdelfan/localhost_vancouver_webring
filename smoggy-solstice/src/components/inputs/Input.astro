---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"input"> {
    icon?: string;
}

const { icon, ...props } = Astro.props;
---

<input {...props} class="bg-skin-secondary min-w-72 py-1.5 px-3 " />
